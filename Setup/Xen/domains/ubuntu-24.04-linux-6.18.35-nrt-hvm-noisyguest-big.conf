# =====================================================================
# Example PVH Linux guest configuration
# =====================================================================
#
# This is a fairly minimal example of what is required for a
# PVH Linux guest. For a more complete guide see xl.cfg(5)

# This configures a PVH rather than PV guest
type = "hvm"

# Guest name
name = "ubuntu-24.04-linux-6.18.35-noisyguest-big"

# 128-bit UUID for the domain as a hexadecimal number.
# Use "uuidgen" to generate one if required.
# The default behavior is to generate a new UUID each time the guest is started.
#uuid = "XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX"

# Kernel image to boot
kernel = "/boot/vmlinuz-6.18.35"

# Ramdisk (optional)
ramdisk = "/boot/initrd.img-6.18.35"

# Kernel command line options
extra = "root=/dev/xvda console=hvc0"

# Initial memory allocation (MB)
memory = 16384
maxmem = 16384

# Number of VCPUS
cpus = "4-23"
vcpus = 16
maxvcpus = 16

# Network devices
# A list of 'vifspec' entries as described in
# docs/misc/xl-network-configuration.markdown
#vif = [ 'bridge=xenbr0' ]

# Disk Devices
# A list of `diskspec' entries as described in
# docs/misc/xl-disk-configuration.txt
disk = [ '/dev/ubuntu-vg/ubuntu-24.04-domU-clone,raw,xvda,rw' ]

# sdl = 1
# vnc = 1
